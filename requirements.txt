# requirements.txt

# Core dependencies
python-binance>=1.0.19 # Check latest stable version
pandas>=1.5.0 # Or latest compatible
numpy>=1.23.0 # Or latest compatible
PyYAML>=6.0
schedule>=1.1.0
requests>=2.28.0
beautifulsoup4>=4.11.0
plotly>=5.10.0
scikit-learn>=1.1.0

# Web Panel
streamlit>=1.20.0 # Or latest compatible

# Notifications
apprise>=1.1.0 # Or latest compatible

# Process Manager for Render
honcho>=1.1.0

# Optional: TensorFlow for LSTM (Install manually or ensure sufficient resources)
# tensorflow>=2.10.0 # Uncomment if using TensorFlow and ensure build environment supports it

# For local .env loading (optional)
python-dotenv>=0.21.0

